[project]
name = "<cache-name>"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "datalad",
    "datalad-container",
    # TODO: add this cache's processing dependencies used by code/update.py.
]
